#enable colors
export CLICOLOR=1

# some alias
alias la='ls -a'
alias vim='/usr/local/Cellar/vim/8.0.0531/bin/vim'

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
source ~/.profile
