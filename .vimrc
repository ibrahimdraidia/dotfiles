syntax enable " enable syntax procesing
set tabstop=4 " number of visual spaces per TAB
set softtapstop=4 " number of spaces in tab when editing
set expandtab " tabs are spaces
set number " show line number
set showcmd " show command in bottom bar
set cursorline " highlight current line
filetype indent on "  load filetype specific indent  files ex: .vim/indent/c.vim
set wildmenu " visual autocomplete for command menu
set lazyredraw " redraw only when we need to
set showmatch " highlight matching
set incsearch  " search as characters are  entreted 
set hlsearch " highlight matches 

